<UserControl
    x:Class="ZapretGUI.App.Controls.InfoBanner"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    x:Name="Root"
    d:DesignWidth="500">

    <Border x:Name="Root_Border" CornerRadius="8" Padding="14,12" BorderThickness="1,1,1,1">
        <Border.BorderBrush>
            <SolidColorBrush Color="{DynamicResource Color.Info}"/>
        </Border.BorderBrush>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Path x:Name="IconPath" Grid.Column="0" Width="18" Height="18" Stretch="Uniform"
                  VerticalAlignment="Top" Margin="0,1,10,0"/>

            <StackPanel Grid.Column="1">
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock x:Name="MessageText" Opacity="0.85" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
